<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>JobWhisper - Moving Brains | Find Your Dream Job</title>

    <!-- Favicon -->
    <link rel="icon" type="image/png" href="/images/favicon.png">

    <!-- CSS -->
    <link rel="stylesheet" href="/css/styles.css">

    <!-- Icons (Lucide via CDN) -->
    <script src="https://unpkg.com/lucide@latest"></script>
</head>

<body>

    <!-- ================= NAVBAR ================= -->
    <nav class="navbar">
        <div class="container">
            <!-- Logo -->
            <a href="/" class="brand-logo-container" style="padding-top: 0;">
                <img src="/images/logo-header.png" alt="JobWhisper" style="height: 60px; object-fit: contain;">
            </a>

            <!-- Desktop Links -->
            <div class="nav-links">
                <a href="/">Home</a>
                <a href="/jobs">Find Jobs</a>
                <a href="#">Career Advice</a>
                <a href="/employer-dashboard">Employers</a>
                <a href="/contact">Contact</a>
            </div>

            <!-- Actions -->
            <div class="nav-actions">
                <% if (typeof user !=='undefined' && user) { %>
                    <% if (user.role==='employer' ) { %>
                        <a href="/employer-dashboard" class="btn btn-outline"
                            style="min-width: unset; padding: 0.5rem 1rem;">Dashboard</a>
                        <% } else { %>
                            <a href="/candidate-dashboard" class="btn btn-outline"
                                style="min-width: unset; padding: 0.5rem 1rem;">Dashboard</a>
                            <% } %>
                                <a href="/logout" class="btn btn-outline"
                                    style="min-width: unset; padding: 0.5rem 1rem;">Logout</a>
                                <% if (user.role==='employer' ) { %>
                                    <a href="/post-job" class="btn btn-primary">Post a Job</a>
                                    <% } %>
                                        <% } else { %>
                                            <a href="/login" class="btn btn-outline">Sign In</a>
                                            <a href="/signup" class="btn btn-primary">Sign Up</a>
                                            <% } %>
                                                <button class="mobile-menu-btn" id="mobileMenuBtn">
                                                    <i data-lucide="menu"></i>
                                                </button>
            </div>
        </div>
    </nav>